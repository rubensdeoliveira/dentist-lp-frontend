export type ThemeModel = {
  colors: {
    background: string
    primary: string
    font: string
    fontAlt: string
    button: string
  }
  fonts: {
    family: {
      title: string
      body: string
    }
  }
}
